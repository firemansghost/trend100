# Copy to .env.local for local overrides. Do not commit .env.local.

# Data provider: "mock" or "marketstack"
DATA_PROVIDER=mock

# Marketstack API key (required if DATA_PROVIDER=marketstack)
# Get your key from: https://marketstack.com/
MARKETSTACK_API_KEY=your_key_here
